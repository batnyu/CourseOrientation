<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="5dip" />
            <gradient
                android:startColor="@color/fondProgressBar"
                android:centerColor="@color/fondProgressBar"
                android:endColor="@color/fondProgressBar"
                />
        </shape>
    </item>


    <item
        android:id="@android:id/progress"
        >
        <clip>
            <shape>
                <corners
                    android:radius="5dip" />
                <gradient
                    android:startColor="@color/colorMain"
                    android:centerColor="@color/colorJaune"
                    android:endColor="@color/colorRouge" />
            </shape>
        </clip>
    </item>

</layer-list>